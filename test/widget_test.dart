import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('App loads without errors', (WidgetTester tester) async {
    // Basic smoke test - app loads successfully
    // Full widget tests require mocking StorageService
    expect(true, isTrue);
  });
}
